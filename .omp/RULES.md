# Boundaries

- The Poppy app repo (`~/Projects/Poppy`, including `Poppy - 2025-Q4`) is OFF
  LIMITS. Never read, write, grep, glob, or run shell commands against it —
  even when a memory hit or a user topic (for example a "Nit" about a progress
  bar) seems to point there. Ask the user first; Poppy work happens only in
  Poppy sessions.
- Never leave this workspace's repo without explicit user confirmation.
